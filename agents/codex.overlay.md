## Codex overlay

- Global defaults live in ~/.codex/AGENTS.md.
- Keep project-specific instructions in repo-local AGENTS.md only when they truly differ from the global base.
- If the user asks for a plan or design, return a concise plan first and wait for approval before editing.
- Execute one bounded implementation step at a time. Multiple files are fine when they belong to one coherent step.
- Re-ground on the plan and changed files before continuing after a failed step.