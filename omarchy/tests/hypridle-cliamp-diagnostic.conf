general {
    inhibit_sleep = 0
}

listener {
    timeout = 3
    on-timeout = logger -t cliamp-idle-diagnostic timeout && ~/.local/bin/omarchy-cliamp-screensaver start
    on-resume = logger -t cliamp-idle-diagnostic resume && ~/.local/bin/omarchy-cliamp-screensaver resume "$(date +%s%N)"
}
